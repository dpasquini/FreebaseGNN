
class BaselineScorer:
    
    def score(self, data, et, edge_index):
        """
        This method should implement the scoring mechanism for the baseline model.
        It takes the graph data, edge type, and edge index as input and returns a score for the given edges. 
        For example, it could return a score based on node degrees, random scores, or any simple heuristic.
        """
        pass